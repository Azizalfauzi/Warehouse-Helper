<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.d3ifcool.warehousehelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pinjambarang.TambahDataPeminjaman"></activity>
        <activity android:name=".inventaris.CariData" />
        <activity android:name=".tambahdata.TambahData" />
        <activity android:name=".Dashboard.DashboardActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".autentifikasi.SignUpActivity" />
        <activity android:name=".autentifikasi.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>